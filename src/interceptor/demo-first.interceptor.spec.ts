import { DemoFirstInterceptor } from './demo-first.interceptor';

describe('DemoFirstInterceptor', () => {
  it('should be defined', () => {
    expect(new DemoFirstInterceptor()).toBeDefined();
  });
});
