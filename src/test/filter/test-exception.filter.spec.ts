import { TestExceptionFilter } from './test-exception.filter';

describe('TestExceptionFilter', () => {
  it('should be defined', () => {
    expect(new TestExceptionFilter()).toBeDefined();
  });
});
