import { TestFirstInterceptor } from './test-first.interceptor';

describe('TestFirstInterceptor', () => {
  it('should be defined', () => {
    expect(new TestFirstInterceptor()).toBeDefined();
  });
});
