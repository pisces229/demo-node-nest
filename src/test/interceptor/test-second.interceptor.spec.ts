import { TestSecondInterceptor } from './test-second.interceptor';

describe('TestSecondInterceptor', () => {
  it('should be defined', () => {
    expect(new TestSecondInterceptor()).toBeDefined();
  });
});
