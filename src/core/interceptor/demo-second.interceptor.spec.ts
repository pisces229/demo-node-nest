import { DemoSecondInterceptor } from './demo-second.interceptor';

describe('DemoSecondInterceptor', () => {
  it('should be defined', () => {
    expect(new DemoSecondInterceptor()).toBeDefined();
  });
});
