import { DemoExceptionFilter } from './demo-exception.filter';

describe('DemoExceptionFilter', () => {
  it('should be defined', () => {
    expect(new DemoExceptionFilter()).toBeDefined();
  });
});
